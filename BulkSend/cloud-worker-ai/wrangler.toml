name = "bulksender-ai-agent"
main = "index.js"
compatibility_date = "2024-03-01"
# Configure GEMINI_API_KEY via `wrangler secret put GEMINI_API_KEY`
# or a local `.dev.vars` file that is kept out of git.

[vars]
CHATSPROMO_GEMINI_MODEL = "gemini-2.5-flash"
